name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  fastcheck:
    name: Fast check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4

      - name: Generate config.h
        run: make config.h

      - name: Build tools and libraries
        run: make -j$(nproc 2>/dev/null || sysctl -n hw.ncpu) all

      - name: Run tests (no valgrind)
        run: make fastcheck -k -j$(nproc 2>/dev/null || sysctl -n hw.ncpu)

  fastcheck-32bit:
    name: Fast check (ubuntu-latest, 32-bit)
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Install 32-bit toolchain
        run: sudo apt-get update && sudo apt-get install -y gcc-multilib g++-multilib libc6-dev-i386

      - name: Generate config.h
        run: make CFLAGS=-m32 config.h

      - name: Build tools and libraries
        run: make CFLAGS=-m32 -j$(nproc) all

      - name: Run tests (no valgrind)
        run: make CFLAGS=-m32 fastcheck -k -j$(nproc)

  valgrind:
    name: Full check with valgrind
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.ref == 'refs/heads/master'

    steps:
      - uses: actions/checkout@v4

      - name: Install valgrind
        run: sudo apt-get update && sudo apt-get install -y valgrind

      - name: Generate config.h
        run: make config.h

      - name: Build tools and libraries
        run: make -j$(nproc) all

      - name: Run full tests
        run: make check -k -j$(nproc)
